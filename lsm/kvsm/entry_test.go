package kvsm

import (
	"testing"
)

func TestLSMKVSMEntryResetValue(t *testing.T) {
	// e := newEntry("content")
	// if e.GetValue().(string) != "content" {
	// 	t.Errorf("Invalid content")
	// }
	// e.ValueReset()
	// if e.GetValue() != nil {
	// 	t.Errorf("Can't store nil")
	// }
}
